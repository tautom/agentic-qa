[pytest]
addopts = -ra --strict-markers --browser chromium
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
markers =
    smoke: critical happy-path checks
    regression: broader business flow coverage
    checkout: checkout and order-completion scenarios
    cart: shopping cart scenarios
    login: authentication scenarios
